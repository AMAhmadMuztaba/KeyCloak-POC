# Bundled in the provider JAR (not an env var) so redeploying only needs the
# same scp+kc.sh build+restart already used for every other provider change,
# no container recreation needed to add/rotate an env var. Rotate by editing
# this file and redeploying — same value must be set on the API side as
# Onboarding:ProfilePictureSharedSecret (OpenBao: Onboarding__ProfilePictureSharedSecret).
api-base-url=https://blocks-ms.inb.seliselocal.com/AutomationWebService/v2
profile-picture-shared-secret=nbxJoOzXLXlD7l33cDwBwb2aGhP4Msh-X4FMUPoO3MY
